import type { SxProps, Theme } from '@mui/material';

export type GridMaintextSxType = {
    sx: SxProps<Theme>;
};

export type ContainerMainDestinationSxType = {
    boxContainer: SxProps<Theme>;
};

export type PresentationContainerSxType = {
    boxContainer: SxProps<Theme>;
    boxImage: SxProps<Theme>;
    gridCustom: SxProps<Theme>;
};

export type MainTextSxType = {
    mainTextContainer: SxProps<Theme>;
    boxTitle: SxProps<Theme>;
    typographySx: SxProps<Theme>;
};
