import type { SxProps, Theme } from '@mui/material';

export type ExperienceComponentSxType = {
    cardSx: SxProps<Theme>;
    boxTitleSx: SxProps<Theme>;
    boxAlertSx: SxProps<Theme>;
};

export type UnbelivableExperiencesSxType = {
    boxTitleSx: SxProps<Theme>;
    boxMobileSx: SxProps<Theme>;
};
