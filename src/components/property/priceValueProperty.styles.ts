import type { Theme } from '@mui/material';

export const priceValueSx = (theme: Theme) => ({
    priceValueTypoSx: {
        fontSize: 30,
    },
    priceValueboxSx: {
        fontSize: 12,
        opacity: 0.7,
        letterSpacing: 1.3,
    },
});
