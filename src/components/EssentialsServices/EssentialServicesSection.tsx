import type { ReactElement } from 'react';

export default function EssentialServicesSection(props: {
     className: string;
}): ReactElement {
     return (
          <div
               className={props.className}
               style={{ width: '100%', height: '100vw' }}
          >
               a asdasd
          </div>
     );
}
