import type { SxProps, Theme } from '@mui/material';

export type HeaderDesktopSxType = {
    stackSx: SxProps<Theme>;
    logoSx: SxProps<Theme>;
    boxSx: SxProps<Theme>;
};

export type HeaderMobileSxType = {
    stackSx: SxProps<Theme>;
    navSx: SxProps<Theme>;
};
